
com.google.gson.JsonElement ${localVarName} = ${jsonParamName};
if (${localVarName} != null && !${localVarName}.isJsonNull()) {
    com.google.gson.JsonObject ${itemName} = ${localVarName}.getAsJsonObject();
    java.util.Set ${setName} = ${itemName}.entrySet();
    java.util.Iterator ${iteratorName} =  ${setName}.iterator();
    ${instanceFieldName}  = new ${mapType}();
    while(${iteratorName} .hasNext()){
        java.util.Map.Entry ${entryName} = (java.util.Map.Entry)${iteratorName} .next();
        ${valueType} ${valueName} = null;
        ${valueSetter}
        toMap.put(${entryName}.getKey().toString(), ${valueName});
    }
}